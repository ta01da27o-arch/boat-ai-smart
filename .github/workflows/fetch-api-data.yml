name: 🔄 Fetch Race Data

on:
  workflow_dispatch:  # 手動実行用

jobs:
  fetch-data:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: 🧩 Node.js をセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 パッケージをインストール
        run: |
          cd server
          npm install node-fetch@3 cheerio

      - name: 🚀 レースデータを取得
        run: |
          cd server
          node fetchApiPrograms.js

      - name: 🧾 JSONファイルを確認
        run: |
          echo "=== data.json ==="
          cat server/data/data.json || echo "data.json not found"
          echo "================"

      - name: 🪄 Git 設定
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

      - name: 💾 変更をコミット
        run: |
          git add server/data/data.json
          git commit -m "🔄 Update race data via external API" || echo "No changes to commit"

      - name: 🚀 GitHubへプッシュ
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git main