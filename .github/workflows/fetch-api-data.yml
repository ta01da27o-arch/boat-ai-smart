name: Fetch Race Data (External API)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"  # JST12:00頃に自動実行（UTC3:00）

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: 🧭 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm install

      - name: 🚀 Fetch race data from external API
        run: node server/fetchApiPrograms.js

      - name: 💾 Commit and Push updated data
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add server/data/*.json
          git commit -m "🔄 Update race data via external API" || echo "No changes"
          git push